// JavaScript Document
/* --------------------------------------------
 FIXED MENU ON SCROLL
-------------------------------------------- */
jQuery(document).ready(function($) {
	'use strict';
	$("#sticky-section").sticky({topSpacing:0});
});